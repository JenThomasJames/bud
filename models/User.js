class User {
  constructor(
    id,
    firstName,
    lastName,
    phone,
    cash,
    pin,
    cardIds, //array with card IDs
    contactIds, // array with user IDs
    favoriteIds, // array with user IDs of favorite contacts
    transaction
  ) {}
}
